-----------------------Create SuperUser on Ubuntu---------
$ sudo adduser USERNAME
$ usermod -aG sudo USERNAME
-----------------------------------------------------------
sudo psswd [username]	: change password
